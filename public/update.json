{
  "version": "2.7.50",
  "changelog": [
    "FIXED: Load & Bill failed to load — each API fetch is now independent (one failing doesn't break all)",
    "FIXED: Shop Card print — cards now centered on A4 page (justify-content: center)",
    "FIXED: Check Update — now ALWAYS fetches from GitHub Releases API (not local latest.yml)",
    "FIXED: Check Update — if newer version found, opens download URL in browser",
    "NEW: Electron preload — openExternal IPC handler for opening URLs in browser",
    "FIXED: Preload version updated from 2.7.31 to 2.7.50"
  ]
}
